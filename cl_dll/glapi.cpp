# pragma comment(lib,"opengl32.lib")

#include <windows.h>
#include <GL/gl.h>
#include <GL/glu.h>
#include <GL/glaux.h>

void RallyFog(void)
{
	/*float colour[2];
	colour[0] = 128;
	colour[1] = 96;
	colour[2] = 64;

	glClearColor(0.5f,0.5f,0.5f,1.0f);

	glFogi(GL_FOG_MODE, GL_LINEAR); // Fog Mode
	glFogfv(GL_FOG_COLOR, colour);
	glFogf(GL_FOG_DENSITY, (GLfloat)0.25f);
	glHint(GL_FOG_HINT, GL_NICEST);
	glFogf( GL_FOG_START, (GLfloat)30 );
	glFogf( GL_FOG_END, (GLfloat)9000 );
	glEnable(GL_FOG);*/


}